import styled from "styled-components";

const PlanetStats = ({
    rotation,
    revolution,
    radius,
    temp,
}) => {
    return (
        <Container></Container>
    );
}

export default PlanetStats;

const Container = styled.div``;